Bonjour <%= @user.first_name.capitalize %> !

<p>Voici un récapitulatif de toutes les séances que tu vas coacher dans les prochaines 24 heures :

<% if @lessons.empty? %>
    Aucune séance n'est prévue.
<% else %>
  <% @lessons.each do |lesson| %>
    <%= lesson.sport_type %> - <%= lesson.date.localtime.strftime("le %d/%m/%Y à %k:%M") %> - <%= lesson.location.name %>
  <% end %>
<% end %>

<%= link_to 'Mes coachings', profile_url(tab: 'tab-1'), class:'btn-red mb-3', target:'_blank', rel:'noopener noreferrer' %>

À très vite
